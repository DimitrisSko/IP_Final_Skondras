from django.apps import AppConfig

class AccountsConfig(AppConfig):

   # Configuration for the 'accounts' Django application.

   # This class is used to configure application-specific settings for the 'accounts' app.

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'accounts'  # The name of the Django app, used for referring to this app in the project
